## 2026-03-23 - initialization
- Trigger: PROJECT_BOOTSTRAPPED
- Regla aprendida: Registrar toda spec con el CLI del docs registry y no editar el JSONL manualmente.
- Prevencion futura: Validar que `featureSlug`, `docId` y `path` esten alineados antes de publicar `DOC_UPDATED`.
