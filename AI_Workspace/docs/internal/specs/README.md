# Specs internas

Aqui viven las especificaciones internas del proyecto.

Buenas practicas:

- nombrar los archivos con un `featureSlug` claro,
- mantenerlos trazables a un `taskId`,
- registrar toda spec en `docs/internal/registry/docs_registry.jsonl` con `node scripts/docs-registry.js register ...`.
